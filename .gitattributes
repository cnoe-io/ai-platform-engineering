# Shell scripts must use LF so Linux containers can exec them (Windows CRLF breaks shebang)
*.sh text eol=lf

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# This TypeScript source historically contained a literal NUL separator.
ui/src/app/api/admin/users/*/access/route.ts diff
