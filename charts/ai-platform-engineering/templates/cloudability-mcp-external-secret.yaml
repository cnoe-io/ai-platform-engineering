{{- if and .Values.cloudabilityMcp.enabled .Values.cloudabilityMcp.externalSecrets.enabled }}
{{- $externalSecrets := .Values.cloudabilityMcp.externalSecrets -}}
{{- $secretStoreRef := $externalSecrets.secretStoreRef | default .Values.global.externalSecrets.secretStoreRef -}}
apiVersion: external-secrets.io/{{ $externalSecrets.apiVersion | default (include "ai-platform-engineering.externalSecrets.apiVersion" .) }}
kind: ExternalSecret
metadata:
  name: {{ include "ai-platform-engineering.cloudabilityMcp.fullname" . }}
  labels:
    {{- include "ai-platform-engineering.cloudabilityMcp.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ $externalSecrets.refreshInterval | default "1h" | quote }}
  secretStoreRef:
    {{- toYaml $secretStoreRef | nindent 4 }}
  target:
    name: {{ include "ai-platform-engineering.cloudabilityMcp.secretName" . }}
    creationPolicy: Owner
  data:
    {{- toYaml $externalSecrets.data | nindent 4 }}
{{- end }}
