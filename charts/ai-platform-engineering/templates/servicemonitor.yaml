{{- $daPort := index .Values "dynamic-agents" "service" "port" | default 8001 | int }}
{{- $daMetricsPort := index .Values "dynamic-agents" "service" "metricsPort" | default 0 | int }}
{{- $daMetricsSeparate := and (ne $daMetricsPort 0) (ne $daMetricsPort $daPort) }}
{{- if .Values.metrics.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-metrics
  labels:
    app.kubernetes.io/name: ai-platform-engineering
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- with .Values.metrics.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.metrics.serviceMonitor.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  # Match services that expose /metrics on their http port. Services excluded
  # by name have dedicated per-component ServiceMonitors targeting the correct
  # port for their metrics endpoint:
  #   - agentgateway: servicemonitor-agentgateway.yaml (admin port 15000)
  #   - keycloak: servicemonitor-keycloak.yaml (management port 9000)
  #   - openfga: servicemonitor-openfga.yaml (metrics port 2112)
  #   - dynamic-agents (only when dynamic-agents.service.metricsPort is set
  #     to a port different from the main port): servicemonitor-dynamic-agents.yaml
  # audit-service has no /metrics endpoint and is excluded entirely.
  # caipe-ui is INCLUDED (main http port, default /metrics path) — it now
  # exposes prom-client HTTP + Tome app metrics (ui/src/lib/metrics.ts). It
  # was excluded before that endpoint existed; the Grafana dashboard's
  # caipe-ui panels only started getting real data once this changed.
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: NotIn
        values:
          - mcp
          - rag-server
          - agent-ontology
          - skill-scanner
      - key: app.kubernetes.io/name
        operator: NotIn
        values:
          - agentgateway
          - keycloak
          - openfga
          - audit-service
          {{- if $daMetricsSeparate }}
          - dynamic-agents
          {{- end }}
  endpoints:
    - port: http
      path: {{ .Values.metrics.path | default "/metrics" }}
      interval: {{ .Values.metrics.serviceMonitor.interval | default "30s" }}
      scrapeTimeout: {{ .Values.metrics.serviceMonitor.scrapeTimeout | default "10s" }}
      {{- with .Values.metrics.serviceMonitor.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.metrics.serviceMonitor.metricRelabelings }}
      metricRelabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
{{- end }}

