{{- if and (not .Values.mongo.existingSecret) (not .Values.mongo.uri) -}}
{{- fail "scheduler.mongo.existingSecret or scheduler.mongo.uri is required; use the same MongoDB as CAIPE UI" -}}
{{- end -}}
