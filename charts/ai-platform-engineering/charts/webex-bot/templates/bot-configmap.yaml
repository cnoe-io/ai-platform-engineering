{{- if .Values.botConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webex-bot.fullname" . }}-bot-config
  labels:
    {{- include "webex-bot.labels" . | nindent 4 }}
data:
  bot-config.yaml: |
    {{- .Values.botConfig | toYaml | nindent 4 }}
{{- end }}
