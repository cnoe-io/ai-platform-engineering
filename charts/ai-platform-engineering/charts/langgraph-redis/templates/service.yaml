apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-redis.fullname" . }}
  labels:
    {{- include "langgraph-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "langgraph-redis.selectorLabels" . | nindent 4 }}
