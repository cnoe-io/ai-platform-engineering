apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-evaluator.fullname" . }}
  labels:
    {{- include "rag-evaluator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-evaluator.selectorLabels" . | nindent 4 }}
