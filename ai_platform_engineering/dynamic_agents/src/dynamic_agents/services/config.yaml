# Dynamic Agents Seed Configuration
# Generated from caipe-preview Kubernetes cluster

# =============================================================================
# Available LLM Models
# =============================================================================

models:
  - description: Fast, efficient Claude 4.5 Haiku via AWS Bedrock
    model_id: global.anthropic.claude-haiku-4-5-20251001-v1:0
    name: Claude 4.5 Haiku (Bedrock)
    provider: aws-bedrock

  - description: Balanced performance Claude Sonnet 4 via AWS Bedrock
    model_id: global.anthropic.claude-sonnet-4-20250514-v1:0
    name: Claude Sonnet 4 (Bedrock)
    provider: aws-bedrock

  - description: Latest Claude Sonnet 4.5 via AWS Bedrock
    model_id: global.anthropic.claude-sonnet-4-5-20250929-v1:0
    name: Claude Sonnet 4.5 (Bedrock)
    provider: aws-bedrock

  - description: Claude 3.5 Sonnet v2 via AWS Bedrock
    model_id: global.anthropic.claude-3-5-sonnet-20241022-v2:0
    name: Claude 3.5 Sonnet v2 (Bedrock)
    provider: aws-bedrock

  - description: GPT-4o via Azure OpenAI
    model_id: gpt-4o
    name: GPT-4o (Azure)
    provider: azure-openai

  - description: Fast, cost-effective GPT-4o Mini via Azure OpenAI
    model_id: gpt-4o-mini
    name: GPT-4o Mini (Azure)
    provider: azure-openai

# =============================================================================
# Seed MCP Servers
# =============================================================================

mcp_servers:
  - description: ArgoCD application and deployment management
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-argocd-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: argocd
    name: ArgoCD
    transport: http

  - description: Backstage catalog and developer portal
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-backstage-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: backstage
    name: Backstage
    transport: http

  - description: Confluence wiki and documentation
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-confluence-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: confluence
    name: Confluence
    transport: http

  - description: Jira issue tracking and project management
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-jira-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: jira
    name: Jira
    transport: http

  - description: Network diagnostic tools (ping, traceroute, DNS lookup)
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-netutils-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: netutils
    name: Network Utilities
    transport: http

  - description: PagerDuty incident management
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-pagerduty-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: pagerduty
    name: PagerDuty
    transport: http

  - description: Slack messaging and collaboration
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-slack-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: slack
    name: Slack
    transport: http

  - description: Splunk log analysis and monitoring
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-splunk-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: splunk
    name: Splunk
    transport: http

  - description: Webex messaging and meetings
    enabled: true
    endpoint: http://a-caipe-preview-argoapp-agent-webex-mcp.caipe-preview.svc.cluster.local:8000/mcp
    id: webex
    name: Webex
    transport: http

  - description: Knowledge Base tools
    enabled: true
    endpoint: http://rag-server.caipe-preview.svc.cluster.local:9446/mcp
    id: knowledge-base
    name: Knowledge Base
    transport: http

# =============================================================================
# Seed Agents
# =============================================================================

agents: []
