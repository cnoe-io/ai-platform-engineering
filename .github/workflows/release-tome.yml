name: "[Release] Tome Agent Release"
description: "Tag and build a tome-agent release from the current main HEAD."

# Usage:
#   GitHub UI → Actions → "[Release] Tome Agent Release" → Run workflow
#   gh workflow run release-tome.yml
#
# What this does:
#   1. Reads the nearest semver release tag on main to determine X.Y.Z.
#   2. Computes the short SHA of HEAD.
#   3. Derives image/git tag: X.Y.Z-tome-<sha>
#   4. Bumps ai_platform_engineering/agents/tome/pyproject.toml to that version.
#   5. Commits to main and creates annotated git tag tome-agent-X.Y.Z-tome-<sha>.
#   6. Dispatches ci-tome-agent.yml with tag_version=X.Y.Z-tome-<sha>.
#   7. Creates a draft GitHub release.

on:
  workflow_dispatch: {}
  workflow_call: {}

permissions:
  contents: write
  actions: write

jobs:
  release:
    name: Tag and dispatch build
    runs-on: ubuntu-latest

    steps:
      - name: 🔒 Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: 📥 Checkout main
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Derive version and SHA
        id: ver
        run: |
          set -euo pipefail

          # Nearest semver release tag reachable from HEAD (e.g. 0.5.50)
          BASE=$(git describe --tags --match '[0-9]*.[0-9]*.[0-9]*' --abbrev=0 HEAD 2>/dev/null \
                 | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+' || true)
          if [[ -z "$BASE" ]]; then
            echo "❌ No semver release tag found on main. Tag a release first."
            exit 1
          fi

          SHORT=$(git rev-parse --short HEAD)
          VERSION="${BASE}-tome-${SHORT}"
          GIT_TAG="tome-agent-${VERSION}"

          echo "base=$BASE"       >> "$GITHUB_OUTPUT"
          echo "short=$SHORT"     >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "git_tag=$GIT_TAG" >> "$GITHUB_OUTPUT"
          echo "✅ base=$BASE  sha=$SHORT  version=$VERSION"

      - name: 🔍 Check tag doesn't already exist
        run: |
          set -euo pipefail
          GIT_TAG="${{ steps.ver.outputs.git_tag }}"
          if git ls-remote --exit-code --tags origin "refs/tags/$GIT_TAG" >/dev/null 2>&1; then
            echo "❌ Tag '$GIT_TAG' already exists"
            exit 1
          fi
          echo "✅ Tag '$GIT_TAG' is available"

      - name: 📤 Tag and push
        run: |
          set -euo pipefail
          VERSION="${{ steps.ver.outputs.version }}"
          GIT_TAG="${{ steps.ver.outputs.git_tag }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git tag -a "$GIT_TAG" -m "Tome agent release $VERSION"
          git push origin "$GIT_TAG"
          echo "✅ Pushed tag: $GIT_TAG"

      - name: 🚀 Dispatch ci-tome-agent.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          VERSION="${{ steps.ver.outputs.version }}"
          echo "📦 Dispatching ci-tome-agent.yml with tag_version=$VERSION"
          gh workflow run ci-tome-agent.yml \
            --repo "${{ github.repository }}" \
            --ref main \
            -f tag_version="$VERSION"
          echo "✅ Dispatch sent"

      - name: 📋 Create draft GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          GIT_TAG="${{ steps.ver.outputs.git_tag }}"
          VERSION="${{ steps.ver.outputs.version }}"
          gh release create "$GIT_TAG" \
            --repo "${{ github.repository }}" \
            --title "Tome Agent $VERSION" \
            --generate-notes \
            --draft
          echo "✅ Draft release created: $GIT_TAG"

      - name: 📊 Summary
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          GIT_TAG="${{ steps.ver.outputs.git_tag }}"
          cat >> "$GITHUB_STEP_SUMMARY" << EOF
          ## Tome Agent Release

          | Item | Value |
          |------|-------|
          | Base release | \`${{ steps.ver.outputs.base }}\` |
          | Short SHA | \`${{ steps.ver.outputs.short }}\` |
          | Version | \`$VERSION\` |
          | Git tag | \`$GIT_TAG\` |
          | Image tag | \`$VERSION\` |

          - Image: \`ghcr.io/${{ github.repository_owner }}/caipe-tome-agent:$VERSION\`
          - Draft release: https://github.com/${{ github.repository }}/releases/tag/$GIT_TAG
          EOF
