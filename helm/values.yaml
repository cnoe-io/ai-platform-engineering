# Global configuration shared across all subcharts
global:
  llmSecrets:
    create: false # do not create llm secret in subcharts (use global or existing llm secret)
    secretName: "existing-llm-secret"

######### Supervisor agent configuration #########
supervisor-agent:
  image:
    repository: "ghcr.io/cnoe-io/ai-platform-engineering"
    tag: "latest"
    pullPolicy: "Always"
    args: ["platform-engineer"]

######### Sub-agent configurations #########
# Agent configurations using aliases from Chart.yaml
agent-argocd:
  agentSecrets:
    secretName: "argocd-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-backstage:
  agentSecrets:
    secretName: "backstage-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-confluence:
  agentSecrets:
    secretName: "agent-confluence-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-github:
  agentSecrets:
    secretName: "github-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-jira:
  agentSecrets:
    secretName: "atlassian-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-pagerduty:
  agentSecrets:
    secretName: "pagerduty-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-slack:
  agentSecrets:
    secretName: "slack-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-aws:
  agentSecrets:
    secretName: "aws-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml
  mcp:
    mode: "stdio"

agent-splunk:
  agentSecrets:
    secretName: "splunk-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-webex:
  agentSecrets:
    secretName: "webex-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml

agent-komodor:
  agentSecrets:
    secretName: "komodor-secret" # Specify an existing Kubernetes secret name, or leave empty to auto-generate from values-secrets.yaml
