# ============================================================================
# AUTO-GENERATED FILE - DO NOT EDIT DIRECTLY
# ============================================================================
# Generated by: scripts/generate-docker-compose.py
# Mode: DEV (with local code mounts)
# Personas: cloudability
# Transports: a2a-p2p, a2a-over-slim
#
# To regenerate this file, run:
#   make generate-docker-compose PERSONAS="cloudability" DEV=true
#
# Or manually:
#   ./scripts/generate-docker-compose.py --persona cloudability --dev
#
# Usage:
#   docker compose -f docker-compose/docker-compose.cloudability.yaml --profile a2a-p2p up         # A2A peer-to-peer transport
#   docker compose -f docker-compose/docker-compose.cloudability.yaml --profile a2a-over-slim up   # A2A over SLIM transport
# ============================================================================

services:
  caipe-cloudability-p2p:
    container_name: caipe-cloudability-p2p
    volumes:
    - ../prompt_config.yaml:/app/prompt_config.yaml
    - ../persona.yaml:/app/persona.yaml
    - ../ai_platform_engineering:/app/ai_platform_engineering
    env_file:
    - ../.env
    ports:
    - 8000:8000
    environment:
    - A2A_TRANSPORT=p2p
    - CLOUDABILITY_AGENT_HOST=agent-cloudability-cloudability-p2p
    - ENABLE_ARGOCD=false
    - ENABLE_AWS=false
    - ENABLE_BACKSTAGE=false
    - ENABLE_CONFLUENCE=false
    - ENABLE_GITHUB=false
    - ENABLE_GITLAB=false
    - ENABLE_JIRA=false
    - ENABLE_KOMODOR=false
    - ENABLE_PAGERDUTY=false
    - ENABLE_SLACK=false
    - ENABLE_SPLUNK=false
    - ENABLE_CLOUDABILITY=true
    - ENABLE_WEATHER_AGENT=false
    - ENABLE_WEBEX_AGENT=false
    - ENABLE_PETSTORE_AGENT=false
    - ENABLE_RAG=false
    depends_on:
    - agent-cloudability-cloudability-p2p
    command: platform-engineer
    build:
      context: ..
      dockerfile: build/Dockerfile
    profiles:
    - a2a-p2p
  agent-cloudability-cloudability-p2p:
    container_name: agent-cloudability-cloudability-p2p
    env_file:
    - ../.env
    ports:
    - 8001:8000
    environment:
    - A2A_TRANSPORT=p2p
    - MCP_MODE=http
    - MCP_HOST=mcp-cloudability
    - MCP_PORT=8000
    depends_on:
    - mcp-cloudability
    volumes:
    - ../ai_platform_engineering/agents/cloudability/agent_cloudability:/app/ai_platform_engineering/agents/cloudability/agent_cloudability
    - ../ai_platform_engineering/agents/cloudability/clients:/app/ai_platform_engineering/agents/cloudability/clients
    - ../ai_platform_engineering/utils:/app/ai_platform_engineering/utils
    build:
      context: ..
      dockerfile: build/agents/Dockerfile.a2a
      args:
      - AGENT_NAME=cloudability
      - AGENT_PACKAGE=cloudability
    profiles:
    - a2a-p2p
  mcp-cloudability:
    container_name: mcp-cloudability
    env_file:
    - ../.env
    ports:
    - 18085:8000
    environment:
    - MCP_MODE=http
    - MCP_HOST=0.0.0.0
    - MCP_PORT=8000
    volumes:
    - ../ai_platform_engineering/agents/cloudability/mcp/mcp_cloudability:/app/mcp_cloudability
    build:
      context: ..
      dockerfile: build/agents/Dockerfile.mcp
      args:
      - AGENT_NAME=cloudability
    profiles:
    - a2a-p2p
    - a2a-over-slim
  caipe-cloudability-slim:
    container_name: caipe-cloudability-slim
    volumes:
    - ../prompt_config.yaml:/app/prompt_config.yaml
    - ../persona.yaml:/app/persona.yaml
    - ../ai_platform_engineering:/app/ai_platform_engineering
    env_file:
    - ../.env
    ports:
    - 8000:8000
    environment:
    - A2A_TRANSPORT=slim
    - CLOUDABILITY_AGENT_HOST=agent-cloudability-cloudability-slim
    - ENABLE_ARGOCD=false
    - ENABLE_AWS=false
    - ENABLE_BACKSTAGE=false
    - ENABLE_CONFLUENCE=false
    - ENABLE_GITHUB=false
    - ENABLE_GITLAB=false
    - ENABLE_JIRA=false
    - ENABLE_KOMODOR=false
    - ENABLE_PAGERDUTY=false
    - ENABLE_SLACK=false
    - ENABLE_SPLUNK=false
    - ENABLE_CLOUDABILITY=true
    - ENABLE_WEATHER_AGENT=false
    - ENABLE_WEBEX_AGENT=false
    - ENABLE_PETSTORE_AGENT=false
    - ENABLE_RAG=false
    depends_on:
    - agent-cloudability-cloudability-slim
    - slim-dataplane
    - slim-control-plane
    command: platform-engineer
    build:
      context: ..
      dockerfile: build/Dockerfile
    profiles:
    - a2a-over-slim
  agent-cloudability-cloudability-slim:
    container_name: agent-cloudability-cloudability-slim
    env_file:
    - ../.env
    ports:
    - 8001:8000
    environment:
    - A2A_TRANSPORT=slim
    - MCP_MODE=http
    - MCP_HOST=mcp-cloudability
    - MCP_PORT=8000
    depends_on:
    - mcp-cloudability
    - slim-dataplane
    volumes:
    - ../ai_platform_engineering/agents/cloudability/agent_cloudability:/app/ai_platform_engineering/agents/cloudability/agent_cloudability
    - ../ai_platform_engineering/agents/cloudability/clients:/app/ai_platform_engineering/agents/cloudability/clients
    - ../ai_platform_engineering/utils:/app/ai_platform_engineering/utils
    build:
      context: ..
      dockerfile: build/agents/Dockerfile.a2a
      args:
      - AGENT_NAME=cloudability
      - AGENT_PACKAGE=cloudability
    profiles:
    - a2a-over-slim
  slim-dataplane:
    image: ghcr.io/agntcy/slim:0.3.15
    container_name: slim-dataplane
    profiles:
    - a2a-over-slim
    ports:
    - 46357:46357
    environment:
    - PASSWORD=${SLIM_GATEWAY_PASSWORD:-dummy_password}
    - CONFIG_PATH=/config.yaml
    volumes:
    - ../slim-config.yaml:/config.yaml
    command:
    - /slim
    - --config
    - /config.yaml
  slim-control-plane:
    image: ghcr.io/agntcy/slim/control-plane:0.0.1
    container_name: slim-control-plane
    profiles:
    - a2a-over-slim
    ports:
    - 50051:50051
    - 50052:50052
    environment:
    - PASSWORD=${SLIM_GATEWAY_PASSWORD:-dummy_password}
    - CONFIG_PATH=/config.yaml
    volumes:
    - ../slim-config.yaml:/config.yaml
    command:
    - /slim
    - --config
    - /config.yaml
